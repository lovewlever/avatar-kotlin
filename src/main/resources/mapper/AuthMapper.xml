<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.avatar.model.dao.mapper.AuthMapper">

    <select id="findQQAuthStatus" resultType="INTEGER">
        select count(0) from auth_qq where user_id = #{userId}
    </select>

    <select id="findWXAuthStatus" resultType="INTEGER">
        select count(0) from auth_wx where user_id = #{userId}
    </select>

    <insert id="authQQ" parameterType="com.project.avatar.model.dao.data.AuthQQData">
        insert into auth_qq
        (qq_id, qq_name, user_id) VALUES (#{qqId},#{qqName},#{userId})
    </insert>

</mapper>
